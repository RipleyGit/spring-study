spring:
  application:
    name: nacos-client-config
  cloud:
    nacos:
      discovery:
        server-addr: nacos.server:8848
      config:
        server-addr: nacos.server:8848
        prefix: ${spring.application.name}
        file-extension: yml
        group: SL_GROUP